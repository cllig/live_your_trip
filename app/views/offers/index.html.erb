<h1> All our Offers </h1>
<% @offers.each do | offer | %>
    <h3><%=link_to offer.title, offer_path(offer) %></h3>
    <h3><%= offer.city %></h3> <p><%= offer.user.first_name %></p>
   <p><%= offer.theme %></p>
<% end %>
<%= link_to "Back", root_path %>
