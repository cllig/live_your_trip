<div class='title-card'><h1>Toutes nos offres</h1>
</div>

<div class='align-card'>
<% @offers.each do | offer | %>

    <%= render 'offers/cards', offer: offer %>
<% end %>
</div>
   <%=link_to "See More", offer_path(offer) %>
<%= link_to "Back", root_path %>